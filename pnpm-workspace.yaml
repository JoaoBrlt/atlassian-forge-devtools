# pnpm configuration, see https://pnpm.io/settings

# Minimum age (in minutes) for a package release to be considered valid.
minimumReleaseAge: 4320 # 3 days

# Allowed scripts
allowBuilds:
  esbuild: true
  msw: true
  spawn-sync: true
